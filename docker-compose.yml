version: '3.7'
services:
    postgres:
        image: postgres:10.5
        restart: always
        environment:
            - POSTGRES_USER=PAPP
            - POSTGRES_PASSWORD=SUPERSECRET
        ports:
            - '5438:5432'
